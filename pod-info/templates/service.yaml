apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-info.fullname" . }}
  labels:
    {{- include "pod-info.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pod-info.selectorLabels" . | nindent 4 }}
